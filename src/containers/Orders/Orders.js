import React, { Component } from "react";
import classes from "./Orders.module.scss";

class Orders extends Component {
  // state = {  }
  render() {
    return <h1 className={classes.name}>Orders</h1>;
  }
}

export default Orders;
